{
  "name": "@discord-visual-room/types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for Discord Visual Room",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
